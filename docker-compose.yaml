version: '3'
services:
  pinata:
    container_name: "pinata-admin"
    image: "pinata:v1"
    volumes:
      - ./:/usr/src/app
    ports:
      - 5200:5200
      - 5173:5173
    command: sh -c "npm install && npm run client:dev"